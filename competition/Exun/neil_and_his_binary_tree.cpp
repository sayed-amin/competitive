#include <iostream>
#include <vector>
using namespace std;
#define int long long
#define endl "\n"
const int mod = (1e9) + 7;
void solve()
{
   int n, m;
   cin >> n >> m;
   int height = 0;
   while (N)
   {
      N >>= 1;
   }
   vector<vector<int> > dp(height + 1, vector<int>(m + 1, 0));
   for (int i = 2; i <= height;i++){
      if((i&1)==0){
         int sum = 0;
         for (int j = 1; j <= m;j++){

         }
      }
   }
}
int main()
{
   int t;
   cin >> t;
   while (t--)
   {
      int n, m;
      cin >> n >> m;
   }
}